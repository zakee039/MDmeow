[简体中文](README.md) · **English** · [日本語](README.ja.md) · [Deutsch](README.de.md)

# MDmeow

> **Open it. Get to work.**
>
> **A lightweight, polished, review-first tool for Markdown and code documents in the AI era.**

[🌐 Live demo](https://mdmeow.zakee.fun)

In the AI era, more documents start with an Agent, an LLM, a script, or automation—not an empty page.

README files, plans, research notes, JSON, YAML, code snippets and logs increasingly follow the same human workflow:

**open → read → review → make a small edit → save.**

MDmeow is built for that workflow.

It is not an IDE and it is not trying to become a heavyweight knowledge base. It aims to feel as immediate as Notepad, as comfortable as a formatted document viewer, and smart enough to understand Markdown, configuration files and code.

**Double-click the file, read it immediately, edit only when needed.**

That is the core of MDmeow: **open it and get to work.**

## Review first, edit when needed

Markdown opens as a formatted document. JSON, YAML, Python, Rust, JavaScript, configuration files and plain text open directly in a lightweight Code mode.

Syntax colors, line numbers, alternating rows and typography exist to improve review—not to turn MDmeow into another IDE.

Useful for:

- reviewing Markdown generated by AI and agents
- checking README files, plans, research notes and deliverables
- inspecting and lightly editing JSON, YAML, TOML, INI and ENV
- reading scripts, source code, logs and text
- switching between formatted Markdown and source when details matter

## Two modes are enough

### Markdown Render mode

- WYSIWYG view for headings, lists, quotes, tasks, tables, links and footnotes
- images, math and code blocks render directly in the document
- code blocks include syntax highlighting, line numbers, copy feedback and subtle alternating rows
- Ctrl/Cmd + / toggles Markdown source
- files remain ordinary portable Markdown

### Code mode

Non-Markdown text files open in Code mode.

It remains a lightweight text editor. CodeMirror language parsing and the Miku Cream palette simply make structured files easier to scan.

Common formats:

- Config/data: JSON, YAML, XML, TOML, INI, CONF, ENV, JSONL, CSV
- Web: HTML, CSS, SCSS, LESS, JavaScript, TypeScript, JSX, TSX, Vue
- Programming: Python, Rust, C/C++, Java, Go, PHP, SQL, Shell, PowerShell, Ruby, Swift, Kotlin, C#
- Text: TXT, LOG
- Other readable UTF-8 files can fall back to plain text

Code mode provides line numbers, syntax colors, find/replace, save, and optional customizable alternating-row tinting.

**Good to read, capable of editing, not a replacement for VS Code.**

## Miku Cream: designed for comfortable review

- bright, low-noise reading surface
- default accent #39C5BB
- shared semantic code colors between Markdown code blocks and Code mode
- code background matches the surrounding document
- alternating rows default to #FAFFFF and can be customized
- separate fonts and sizes for document and source views
- clear hierarchy for inline code, math, tables and images

The goal is not visual spectacle. It is comfortable reading over long sessions.

## Images, math and code stay inside the document

Images support title, alignment, 25%–200% scaling and deletion. Markdown images and HTML img blocks share the same interaction.

KaTeX renders inline and display math:

~~~text
$E = mc^2$

$$
\int_a^b f(x)\,dx
$$
~~~

Code blocks include syntax highlighting, dedicated line numbers, copy feedback and the same Miku Cream semantic palette used by Code mode.

## Opening files should feel natural

- drag files directly into MDmeow
- multiple tabs
- optional full path in the title area
- optional session restore
- optional window position/size memory
- when position memory is disabled, MDmeow opens centered on the primary display at a sensible size
- Windows file associations can be selected by extension in Settings
- Markdown, config, web, code and text formats can be registered
- “Set as default” registers first, then hands control to Windows Default Apps

MDmeow respects Windows' default-app security model.

## Export, proxy and updates

Markdown can be exported directly to HTML or to PDF through the system print flow.

Remote images and GitHub update traffic can use HTTP/HTTPS or SOCKS5/SOCKS5H proxies with a built-in connection test.

Windows builds:

~~~text
MDmeow-<version>.exe
MDmeow_<version>_x64.msi
~~~

Portable EXE and MSI installer. Linux and macOS release workflows are also included.

## Lightweight by design

![MDmeow lightweight runtime example](docs/assets/screenshots/lightweight.png)

> The screenshot shows an empty document on one Windows machine. Actual resource usage varies with document content, WebView2 and the system environment.

MDmeow is built with **Tauri + Milkdown/Crepe + CodeMirror**.

It does not try to carry the responsibilities of an IDE, a knowledge base and a full writing platform at the same time. Doing less is what makes it useful as a tool you can open all day without thinking about it.

## Shortcuts

| Action | Default |
| --- | --- |
| New tab | Ctrl/Cmd+N |
| Open | Ctrl/Cmd+O |
| Save | Ctrl/Cmd+S |
| Save As | Ctrl/Cmd+Shift+S |
| Close tab | Ctrl/Cmd+W |
| Export HTML / PDF | Ctrl/Cmd+E |
| Toggle Markdown source/render | Ctrl/Cmd+/ |
| Find | Ctrl/Cmd+F |
| Replace | Ctrl/Cmd+H |
| Settings | Ctrl/Cmd+, |

## Configuration

Settings are stored in settings.toml and saved immediately.

~~~toml
language = "system"
spellcheck = true
open_last_session = true
show_path = false

editor_font = ""
editor_font_size = 16
source_font = ""
source_font_size = 15

code_alternate_rows = true
code_alternate_row_color = "#FAFFFF"
remember_window_position = false

accent = "#39C5BB"

proxy_enabled = false
proxy_url = ""
auto_check_updates = true
~~~

## Build from source

Requirements: Node.js 20+, pnpm, Rust stable. Windows also needs Visual C++ Build Tools, Windows SDK and WebView2.

~~~bash
pnpm install
pnpm build
pnpm tauri dev
~~~

~~~powershell
pnpm release:windows
~~~

~~~bash
pnpm release:linux
pnpm release:macos
~~~

## Stack

| Layer | Technology |
| --- | --- |
| Desktop shell | Tauri v2 / Rust |
| Markdown render/edit | Milkdown Crepe / ProseMirror |
| Code mode | CodeMirror |
| Math | KaTeX |
| Markdown → HTML | comrak |
| Export syntax highlighting | highlight.js |
| Windows installer | WiX / MSI |

## Credits

MDmeow is based on **Ali Naderi / Mowl** and remains available under the **MIT License**.

Thanks to the upstream project for an elegant starting point.

MDmeow has since developed its own direction: not doing more for the sake of more, but making Markdown, code and AI-generated documents **faster to open, easier to review, and simple to adjust when needed**.

> **Open it. Get to work.**
