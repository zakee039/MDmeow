![MDmeow](docs/logo.png)

[English](README.md) · [Deutsch](README.de.md) · [فارسی](README.fa.md) · **العربية** · [עברית](README.he.md)

> ℹ️ هذه الترجمة آلية. النسخة الإنجليزية ([README](README.md)) هي المرجع.

# MDmeow

**محرّر Markdown مبسّط ومحمول يعمل بأسلوب "ما تراه هو ما تحصل عليه" (WYSIWYG).**

اكتب Markdown وشاهده منسَّقًا في الحال — على طريقة Typora. قويّ بما يكفي للكتابة
الحقيقية (جداول، معادلات، شيفرة، حواشٍ، الكتابة من اليمين إلى اليسار)، ومع ذلك
ملفّ تنفيذيّ واحد بحجم \~7 ميغابايت يبدأ فورًا ولا يقف في طريقك.

***

## لماذا MDmeow

- **أقوى مما يبدو.** تحرير مباشر (WYSIWYG)، مستندات في ألسنة تبويب، قائمة كتلة،
  جداول GFM مع السحب لإعادة الترتيب، معادلات KaTeX، شيفرة ملوّنة، بحث واستبدال،
  دعم الكتابة من اليمين إلى اليسار، وتصدير HTML/PDF مكتفٍ بذاته.
- **خفيف للغاية.** بلا Electron. MDmeow مبنيّ على [Tauri](https://tauri.app) وعلى
  WebView الخاص بنظام تشغيلك، لذا فالتطبيق كله **ملفّ تنفيذيّ محمول واحد بحجم نحو
  7 ميغابايت** — لا حاجة إلى مثبِّت، لا شيء لفكّ ضغطه، ولا خدمات في الخلفية.
- **سريع حقًّا.** نواته Rust، والنافذة أصلية، وبدء التشغيل البارد فوريّ عمليًّا.
  يبدو كمحرّر نصوص، لا كتطبيق ويب.
- **محمول من الأساس.** ملفّ `settings.toml` واحد قابل للتحرير يدويًّا يقع بجوار
  الملفّ التنفيذيّ. انقل `mdmeow.exe` إلى ذاكرة USB وستنتقل إعداداتك معه.

## المزايا

### ✍️ التحرير المباشر WYSIWYG

مدعوم بـ[Milkdown Crepe](https://milkdown.dev) (ProseMirror). العناوين والخطّ
العريض والقوائم والاقتباسات وبقيّتها تُنسَّق أثناء الكتابة — لكنّ المستند على
القرص يبقى دائمًا Markdown بسيطًا ومحمولًا.

### 🔗 روابط بلا عناء مع الصياغة

حدِّد نصًّا ثم **الصق رابطًا بـ`Ctrl/Cmd+V`** — يصبح المحدَّد نصَّ الرابط ويصبح
الرابط الملصوق هدفه. لا كتابة `[]()`، ولا نافذة حوار. تفضّل لوحة المفاتيح؟ حدِّد
نصًّا واضغط **`Ctrl/Cmd+K`** لتحويله إلى رابط باستخدام أيّ رابط في الحافظة (أو
رابط فارغ لملئه).

### 🖼️ صور تظهر من تلقاء نفسها

`![alt](picture.png)` يُعرَض مباشرةً في المحرّر، بما في ذلك **المسارات النسبية**
التي تُحلّ بالنسبة إلى مجلَّد المستند نفسه (`./assets/diagram.png`،
`../shared/logo.svg`) والمسارات المحلّية المطلقة — لا روابط `http(s)` فقط. أضف
واحدة من قائمة الكتلة `⠿` («Image»)، ثم الصق رابطًا أو اختر ملفًّا.

### 🧱 قائمة الكتلة

مرِّر فوق أيّ كتلة وانقر زرّ `⠿` لقائمة سريعة تعمل على تلك الكتلة:

- **تحويل إلى** — نصّ، عنوان 1–3، قائمة نقطية، قائمة مرقَّمة، اقتباس، كتلة شيفرة،
  أو **جدول**
- **إدراج** جدول أو صورة أو فاصل أو سطر فارغ فوق/تحت
- **تكرار** الكتلة أو **حذفها**

نوع الكتلة الحالية مميَّز حتى تعرف دائمًا ما الذي تحرّره. أنواع «تحويل إلى» لها
أيضًا اختصارات لوحة المفاتيح — `Ctrl/Cmd+0`–`7` (نصّ، عنوان 1–3، قائمة نقطية،
قائمة مرقَّمة، اقتباس، كتلة شيفرة) — تُطبَّق على التحديد تمامًا كما في القائمة.

### 😀 الإيموجي

اضغط `Ctrl/Cmd+.` لمنتقي إيموجي قابل للبحث، أو اكتب `:shortcode:` (مثل
`:tada:` ← 🎉 و`:+1:` ← 👍) فيتحوّل إلى الإيموجي عند إغلاق النقطتين. Unicode
أصليّ — بلا صور ولا تنزيل.

### 📑 ألسنة تبويب مع استعادة الجلسة

افتح عدّة مستندات في ألسنة. أغلق MDmeow وأعد فتحه، وستعود ألسنتك — بل ومواضع
التمرير فيها. (يمكن تعطيله في الإعدادات.)

### 👁️ عرض المصدر

بدِّل بين المحرّر الغنيّ و**Markdown الخام** في منطقة نصّ عادية بـ`Ctrl/Cmd+/`.
`Tab` / `Shift+Tab` يزيدان وينقصان إزاحة الأسطر المحدَّدة، ويظلّ التراجع الأصليّ
يعمل. يُحفَظ موضع القراءة عند التبديل.

### 🔍 البحث والاستبدال

`Ctrl/Cmd+F` للبحث، `Ctrl/Cmd+H` للاستبدال — يعمل في محرّر WYSIWYG وفي عرض المصدر
معًا.

### 📊 جداول متعاونة

جداول GitHub‑Flavored Markdown كاملة. **اسحب الصفوف والأعمدة** لإعادة ترتيبها،
أدرج جدولًا مباشرةً من قائمة الكتلة، والجداول المكتوبة يدويًّا تُحاذى وتُملأ
تلقائيًّا في ملفّ `.md` المحفوظ ليبقى Markdown الخام مقروءًا.

### 🧮 معادلات و💻 شيفرة

- معادلات **KaTeX**، سطرية (`$…$`) وعرضية (`$$…$$`)
- **كتل شيفرة** ملوَّنة نحويًّا مع اكتشاف اللغة

إضافةً إلى بقيّة GFM: قوائم المهام، الحواشي، الشطب، الروابط التلقائية.

### 📤 التصدير

- **HTML مكتفٍ بذاته** — ملفّ واحد بأنماط KaTeX والتلوين مضمَّنة، لا شيء لاستضافته.
  الصور المحلّية تُضمَّن على هيئة data‑URL.
- **PDF** عبر نافذة الطباعة في النظام

### 🎨 السمات والمظهر

سمة فاتحة وداكنة تتبع نظام التشغيل افتراضيًّا، مع مبدِّل يدويّ. خطّ المحرّر وحجم
الخطّ وخطّ عرض المصدر ولون التمييز كلّها قابلة للضبط.

### 🗂️ ربط الملفّات

اجعل MDmeow التطبيق الافتراضيّ لملفّات `.md` / `.markdown` (عبر المثبِّت). النقر
المزدوج على ملفّ Markdown يفتحه في لسان تبويب جديد في النافذة العاملة.

### ⚙️ الإعدادات: واجهة أو ملفّ

يمكن تغيير كلّ إعداد من داخل التطبيق — زرّ الإعدادات في شريط الأدوات (أو
`Ctrl/Cmd+,`) يفتح صفحة الإعدادات فوق المحرّر، بعنصر تحكّم لكلّ خيار، وتُطبَّق
التغييرات وتُحفَظ فور إجرائها. أو حرّر ملفّ `settings.toml` الوحيد الموثَّق
بجوار الملفّ التنفيذيّ في أيّ محرّر نصوص — يلتقط MDmeow **التغيير خلال ثانية، دون
إعادة تشغيل**. إذا كان مجلَّد البرنامج للقراءة فقط، يعود MDmeow إلى مجلَّد إعداد
نظام التشغيل ويخبرك بذلك في النافذة. كلّ إصدار يرافقه أيضًا
`settings.example.toml` موثَّق بالكامل.

### 🌍 الإنجليزية والألمانية

واجهة التطبيق متوفّرة بالإنجليزية والألمانية، وتتبع لغة النظام افتراضيًّا
(`language = "system" | "en" | "de"`، قابلة للتبديل من صفحة الإعدادات).

## Screenshots

| فاتح                                                 | داكن                                              |
| ---------------------------------------------------- | ------------------------------------------------- |
| ![MDmeow أثناء تحرير مستند](docs/screenshot-light.png) | ![MDmeow في الوضع الداكن](docs/screenshot-dark.png) |

![قائمة الكتلة على الزرّ ⠿](docs/screenshot-block-menu.png)

## اختصارات لوحة المفاتيح

| الإجراء                 | الاختصار           |
| ----------------------- | ------------------ |
| لسان تبويب جديد         | `Ctrl/Cmd+N`       |
| فتح                     | `Ctrl/Cmd+O`       |
| حفظ                     | `Ctrl/Cmd+S`       |
| حفظ باسم                | `Ctrl/Cmd+Shift+S` |
| إغلاق اللسان            | `Ctrl/Cmd+W`       |
| تصدير (HTML / PDF)      | `Ctrl/Cmd+E`       |
| تبديل عرض المصدر        | `Ctrl/Cmd+/` |
| بحث                     | `Ctrl/Cmd+F`       |
| استبدال                 | `Ctrl/Cmd+H`       |
| رابط من الحافظة         | `Ctrl/Cmd+K`       |
| لصق رابط على نصّ محدَّد | `Ctrl/Cmd+V`       |
| كتلة: نصّ / عنوان 1–3 / قوائم / اقتباس / شيفرة | `Ctrl/Cmd+0`–`7` |
| إدراج إيموجي            | `Ctrl/Cmd+.`       |
| الإعدادات               | `Ctrl/Cmd+,`       |

## Download

احصل على أحدث بناء من صفحة [Releases](../../releases).

- **ويندوز (x64)** — متاح الآن: `mdmeow.exe` المحمول (\~7 ميغابايت، بلا تثبيت) أو
  مثبِّت NSIS
- **macOS** (x64 + arm64) و**لينكس** (x64 + arm64 بصيغة AppImage) — *قريبًا.*
  خطّ إنتاج الإصدارات متعدّد المنصّات جاهز بالفعل
  ([`.github/workflows/release.yml`](.github/workflows/release.yml))؛ ستصل هذه
  البناءات مع إصدار موسوم لاحق. حتى ذلك الحين، ابنِ من المصدر على المنصّة الهدف
  (قسم [Building](README.md#building)) — MDmeow تطبيق Tauri ويعمل على الثلاثة جميعًا.

البناءات **غير موقَّعة** ولا موثَّقة، لذا قد يحذّر نظام التشغيل عند أوّل تشغيل:

- **ويندوز** — SmartScreen: *More info → Run anyway*
- **macOS** — نقر بالزرّ الأيمن على التطبيق → *Open*، أو
  `xattr -dr com.apple.quarantine /path/to/MDmeow.app`
- **لينكس** — `chmod +x MDmeow*.AppImage` ثم التشغيل

مع كلّ إصدار تُنشَر مجاميع تحقّق SHA‑256.

## الإعداد

`settings.toml` يقع بجوار الملفّ التنفيذيّ (على macOS: بجوار حزمة `.app`)، أو في
مجلَّد إعداد نظام التشغيل كبديل. القسم العلويّ مخصَّص للتحرير اليدويّ ويُعاد
تحميله مباشرةً (تبقى تعليقات `#` بالإنجليزية كما في الملفّ):

```toml
language = "system"         # system (follow the OS) | en | de
spellcheck = true
quit_on_escape = false      # press Esc to quit
list_marker = "*"           # bullet-list marker on save: * | - | +
show_path = false           # show the full file path in the header, not just the name
open_last_session = true    # reopen the previous session's tabs on startup
always_show_tabbar = false  # keep the tab bar visible even with only one file open
editor_font = ""            # WYSIWYG font family (blank = default)
editor_font_size = 16       # headings scale from this
source_font = ""            # Markdown source font (monospace)
source_font_size = 15
accent = "#39C5BB"         # accent colour; reset/default is Miku teal

# below this line: managed by the app — window geometry, open tabs
```

## البناء من المصدر

المتطلّبات:

- Rust (إصدار stable؛ سلسلة أدوات MSVC على ويندوز) — <https://rustup.rs>
- Node 20+ و`pnpm`
- اعتماديّات WebView للمنصّة — <https://tauri.app/start/prerequisites/>

```bash
pnpm install
pnpm tauri dev                       # run with hot reload
pnpm tauri build                     # release bundles for the host OS
pnpm tauri build --bundles nsis      # Windows: installer + portable exe
pnpm exec tsc --noEmit               # frontend typecheck
cargo test --manifest-path src-tauri/Cargo.toml   # Rust unit tests
```

**قطع إصدار:** ارفع `version` في **كلا** الملفّين `package.json` و
`src-tauri/tauri.conf.json`، ثم ادفع وسمًا `v*` — سيبني
`.github/workflows/release.yml` نسخ ويندوز / macOS / لينكس (x64 + arm64) ويفتح
مسوّدة إصدار على GitHub مع مجاميع التحقّق.

توسّع MDmeow؟ اقرأ **[ARCHITECTURE.md](ARCHITECTURE.md)** — يرسم خريطة لكلّ ملفّ
ويوضّح كيف تضيف أزرار شريط الأدوات وعناصر قائمة الكتلة والإعدادات والأوامر.

## حزمة التقنية

| الطبقة                | الاختيار                                                |
| --------------------- | ------------------------------------------------------- |
| القشرة                | [Tauri v2](https://tauri.app) (Rust، WebView النظام)    |
| المحرّر               | [`@milkdown/crepe`](https://milkdown.dev) (ProseMirror) |
| تصدير Markdown ← HTML | [`comrak`](https://github.com/kivikakk/comrak) (Rust)   |
| المعادلات             | [KaTeX](https://katex.org)                              |
