# MDmeow 最终发布方案

> 日期：2026-09-18
> 项目路径：`E:\project\MDmeow`
> 当前版本：`1.7.0`

## 0. 当前已验收状态

以下内容已经完成并由用户验收：

- MDmeow 品牌化与图标替换。
- 顶部左侧移除品牌图标，仅保留文档/标签区域。
- 顶部右侧改为文档操作区 + 设置 / 关于。
- 导出按钮改为明确的“导出”图标。
- 点击导出后，不再弹出系统 Yes / No 对话框，而是显示应用内双行菜单：
  - 导出 HTML
  - 导出 PDF
- 该导出菜单视觉效果已验收，不再调整。
- 默认 / 重置强调色统一为 `#39C5BB`。
- RTL 功能已彻底移除。
- 应用品牌、内部 crate / package / exe 名称已统一为 MDmeow / mdmeow。
- 工作目录已从 `E:\project\mowl` 改为 `E:\project\MDmeow`。
- 旧 Mowl 构建缓存、旧内部标识和无用资源已清理。

---

## 1. 最终发行目标

最终对外发行目录只允许存在两个文件：

```text
release/
├─ MDmeow-1.7.0.exe
└─ MDmeow-1.7.0.msi
```

其中：

- `MDmeow-[版本号].exe`：单文件便携版。
- `MDmeow-[版本号].msi`：Windows 安装版。

明确取消：

- NSIS 安装包。
- ZIP 便携包。
- 对外暴露的无版本号裸 `mdmeow.exe`。
- 对外附带的语言文件、配置模板或其他散件。

`src-tauri/target` 仅作为构建缓存，不属于最终发行目录。

---

## 2. 便携版设计

### 2.1 文件命名

Rust / Tauri 内部构建仍可生成：

```text
src-tauri\target\release\mdmeow.exe
```

发布阶段复制并重命名为：

```text
release\MDmeow-1.7.0.exe
```

版本号必须从项目版本统一读取，禁止硬编码。

### 2.2 便携模式

便携版必须明确进入 Portable 模式，不再通过“程序所在目录是否可写”猜测。

便携版运行后：

```text
MDmeow-1.7.0.exe
settings.toml
data\
```

全部存放在便携 EXE 所在目录。

### 2.3 升级行为

例如用户从：

```text
MDmeow-1.7.0.exe
```

换成：

```text
MDmeow-1.7.1.exe
```

新版本启动时应：

- 检查当前便携注册信息是否仍指向旧版本 EXE。
- 如果当前由便携版拥有注册，则自动更新到当前 EXE 路径。
- 清理自己留下的旧 `MDmeow-*.exe` OpenWith 项。
- 如果存在有效 MSI 安装版，则便携版不得抢占注册。

---

## 3. MSI 安装版设计

### 3.1 只保留 MSI

Tauri bundle 目标只保留：

```json
["msi"]
```

不再生成 NSIS。

最终将 Tauri 生成的 MSI 重命名为：

```text
MDmeow-1.7.0.msi
```

### 3.2 安装后内部程序名

安装后的程序必须使用稳定名称，不带版本号：

```text
C:\Program Files\MDmeow\MDmeow.exe
```

不要安装成 `MDmeow-1.7.0.exe`，避免每次升级改变安装路径和注册命令。

### 3.3 安装模式配置目录

安装版不再使用 exe 目录保存配置。

建议：

```text
配置：
%APPDATA%\MDmeow\settings.toml

本地数据 / 缓存：
%LOCALAPPDATA%\MDmeow\
```

### 3.4 安装标记

MSI 安装时写入正式安装标记，例如：

```text
HKLM\Software\MDmeow

InstallLocation = C:\Program Files\MDmeow
ExecutablePath  = C:\Program Files\MDmeow\MDmeow.exe
Version         = 1.7.0
InstallType     = MSI
```

程序可根据这一套信息明确判断是否为安装版。

---

## 4. MSI 安装器语言

MSI 安装界面使用简体中文。

要求：

- WiX 的中文字符串在构建阶段编译进 MSI。
- 最终发行目录不出现外置 `.wxl`、语言包或其他语言资源文件。
- 用户最终只拿到一个 `MDmeow-[版本号].msi`。

应用自身仍继续支持：

- 简体中文
- English
- Deutsch

安装器语言与应用 UI 语言互不绑定。

---

## 5. 注册表统一设计

安装版与便携版必须维护同一套文件关联身份。

统一 ProgID：

```text
MDmeow.Markdown
```

支持扩展名：

```text
.md
.markdown
.mdx
```

显示名称：

```text
MDmeow Markdown Document
```

统一打开命令格式：

```text
"<MDmeow executable>" "%1"
```

不允许继续存在新的：

```text
MDmeow.md
MDmeow.markdown
Mowl.Markdown
```

等并行 ProgID。

旧 Mowl 注册只允许作为迁移 / 清理兼容逻辑存在。

---

## 6. 注册表优先级

优先级固定：

```text
MSI 安装版
    ↓ 最高优先级
便携版
    ↓ 兜底
无注册
```

### 6.1 MSI 安装时

MSI 安装 / 修复时：

1. 清理旧 `Mowl.Markdown / mowl.exe`。
2. 清理便携版留下的 MDmeow 文件关联。
3. 安装 `MDmeow.exe`。
4. 写入 MSI 安装标记。
5. 注册统一 `MDmeow.Markdown`。
6. Open command 指向安装目录 `MDmeow.exe`。
7. MSI 成为当前注册所有者。

### 6.2 便携版启动时

便携版启动后检查：

1. 是否存在 MSI 安装标记。
2. MSI 标记中的 EXE 是否真实存在。
3. `MDmeow.Markdown` 是否完整指向 MSI 安装版。

如果：

```text
MSI 已安装
且
注册完整有效
```

则便携版不得修改任何文件关联。

只有以下情况之一成立时，便携版才能注册自己：

- 没有安装 MSI。
- MSI 安装标记存在，但对应 EXE 已不存在。
- MSI 文件关联缺失或损坏。

此时注册：

```text
MDmeow.Markdown
→ 当前运行中的 MDmeow-[版本号].exe "%1"
```

### 6.3 MSI 卸载时

MSI 卸载只做：

- 删除安装文件。
- 删除 MSI 安装标记。
- 删除 MSI 当前拥有的 MDmeow 文件关联。

不要：

- 自动寻找旧便携 EXE。
- 自动恢复旧便携路径。

以后用户再次运行便携版，由便携版自行重新注册。

---

## 7. Tauri 现有 File Association 处理

当前 Tauri / WiX 自动产生的：

```text
MDmeow.md
MDmeow.markdown
```

需要移除。

最终 MSI 不再使用 Tauri 自动生成的多 ProgID 文件关联逻辑，而是改为自定义 WiX 注册片段，与程序内部 Windows Integration 共用：

```text
MDmeow.Markdown
```

这样安装版、便携版和设置页的“注册 / 移除”均操作同一套注册表结构。

---

## 8. 设置页“注册到系统打开方式”行为

设置页保留：

```text
注册到系统打开方式    [注册 / 移除]
```

但行为必须遵守安装版优先规则。

### MSI 安装版运行时

- 按钮操作正式安装版注册。
- command 始终指向安装目录 `MDmeow.exe`。

### 便携版运行时

如果检测到有效 MSI：

- 显示系统已有 MDmeow 正式注册。
- 不允许便携版覆盖安装版路径。

如果没有有效 MSI：

- 允许注册当前便携 EXE。
- 允许移除当前便携注册。

---

## 9. 发布构建脚本

新增统一 Windows release 构建流程。

建议顺序：

```text
读取项目版本
↓
检查工作目录
↓
清理旧 target / release
↓
构建前端
↓
Tauri release build（只构建 MSI）
↓
得到内部 mdmeow.exe
↓
复制为 release\MDmeow-[version].exe
↓
复制 / 重命名 MSI 为 release\MDmeow-[version].msi
↓
删除所有其他发行中间产物
↓
检查 release 目录只能有两个文件
↓
计算 SHA256
↓
输出最终发行报告
```

构建失败时不得留下看似完整的 release 目录。

---

## 10. 最终发行校验

发布完成后必须确认：

### 文件

```text
release\MDmeow-x.y.z.exe
release\MDmeow-x.y.z.msi
```

且 release 目录没有第三个文件。

### 便携版

- 单 EXE 可直接运行。
- 创建本地 `settings.toml / data`。
- 没有 MSI 时可注册自身。
- 有有效 MSI 时不覆盖 MSI 注册。
- 移动 EXE 后可自修复便携注册路径。

### MSI

- 安装界面为内嵌简体中文。
- 安装后主程序固定叫 `MDmeow.exe`。
- 配置不落在 Program Files。
- 写入 MSI 安装标记。
- 注册统一 `MDmeow.Markdown`。
- 安装 / 修复可覆盖便携注册。
- 卸载正确清理自己的注册。

### 注册表

最终只使用：

```text
MDmeow.Markdown
```

旧：

```text
Mowl.Markdown
mowl.exe
MDmeow.md
MDmeow.markdown
```

应只作为迁移清理对象，不再新建。

---

## 11. 发布流程习惯

正式发布新版本时继续执行完整流程：

```text
更新版本号
→ build
→ 生成最终两个发行文件
→ Git commit
→ annotated tag
→ push main
→ push tag
→ 校验远端 refs
```

除非明确要求，否则：

- 不发布 NSIS。
- 不发布 ZIP。
- 不把 target 目录加入 Git。
- 不把构建缓存或语言资源散件放进 release。
